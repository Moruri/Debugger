"""TTPython Debugger — a post-mortem and (eventually) live debugger
for distributed time-sensitive applications compiled with Cozaik/TTPython.

The package is organised so that each debugger feature lives in its own
sub-package under ttpython_debugger.features. Today only `deadlines` is
implemented; the other sub-packages are named placeholders so the
structure does not have to change when those features are built.

"""

__version__ = "0.1.0"
