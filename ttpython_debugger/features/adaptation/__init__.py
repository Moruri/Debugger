"""Adaptation explanations (planned, not implemented).

Future scope: given a trace that includes remapping events, explain why
the runtime moved an SQ — which device failed, which alternatives were
considered, which cost model drove the choice.
"""
