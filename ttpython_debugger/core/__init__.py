"""Core primitives shared across debugger features.

Reserved for record types and helpers that cross-cut multiple features
(e.g. a future trace-graph model used by both rootcause and
counterfactual analysis). Nothing lives here yet — today's single
feature (deadlines) is fully self-contained inside its own sub-package.
"""
