"""Command-line entry point for the TTPython debugger.

Each feature registers its own subcommand here. New features add a
module next to deadlines.py and hook it in __main__.py without
touching existing commands.
"""
